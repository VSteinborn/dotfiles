# Browser
browser "flatpak run io.gitlab.librewolf-community %u"

# Vim key binding
## unbind keys
unbind-key j
unbind-key k
## bind keys - vimwiki style
bind-key j down
bind-key k up
bind-key ENTER open
bind-key BACKSPACE quit

## Max items
max-items 10
